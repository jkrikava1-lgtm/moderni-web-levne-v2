---
const projects = [
  {
    title: 'Luxury Fashion',
    category: 'E-commerce',
    year: '2024',
    color: 'from-violet-900/40 to-blue-900/40',
  },
  {
    title: 'FinTech App',
    category: 'Web App',
    year: '2024',
    color: 'from-emerald-900/40 to-teal-900/40',
  },
  {
    title: 'Corporate Rebrand',
    category: 'Firemní web',
    year: '2024',
    color: 'from-amber-900/30 to-orange-900/30',
  },
  {
    title: 'SaaS Platform',
    category: 'Landing Page',
    year: '2023',
    color: 'from-rose-900/30 to-pink-900/30',
  },
  {
    title: 'Real Estate',
    category: 'Prezentace',
    year: '2023',
    color: 'from-blue-900/40 to-indigo-900/40',
  },
];
---

<section id="prace" class="py-32 md:py-44 horizontal-scroll-section">
  <div class="max-w-[1400px] mx-auto px-8 mb-16">
    <div class="flex flex-col md:flex-row md:items-end justify-between gap-6 reveal">
      <div>
        <div class="text-text-muted text-xs tracking-[0.3em] uppercase mb-4">Portfolio</div>
        <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight">
          Vybrané <span class="text-accent">práce</span>
        </h2>
      </div>
      <p class="text-text-secondary max-w-sm text-sm leading-relaxed">
        Každý projekt je unikátní příběh. Zde je výběr těch, na které jsme nejvíc pyšní.
      </p>
    </div>
  </div>

  <!-- Horizontal scrolling cards -->
  <div class="relative overflow-hidden" id="portfolio-container">
    <div class="horizontal-scroll-track px-8" id="portfolio-track">
      {projects.map((project) => (
        <div class="shrink-0 w-[85vw] md:w-[45vw] lg:w-[35vw] group" data-hover>
          <div class={`relative h-[50vh] md:h-[60vh] bg-gradient-to-br ${project.color} rounded-xl overflow-hidden`}>
            <!-- Browser chrome mockup -->
            <div class="absolute top-5 left-5 flex gap-1.5 z-10">
              <div class="w-2 h-2 rounded-full bg-white/15"></div>
              <div class="w-2 h-2 rounded-full bg-white/15"></div>
              <div class="w-2 h-2 rounded-full bg-white/15"></div>
            </div>

            <!-- Mock layout -->
            <div class="absolute inset-0 p-12 pt-16 flex flex-col justify-end">
              <div class="space-y-3">
                <div class="h-4 bg-white/6 rounded w-1/2"></div>
                <div class="h-4 bg-white/6 rounded w-3/4"></div>
                <div class="h-3 bg-white/3 rounded w-full mt-6"></div>
                <div class="h-3 bg-white/3 rounded w-5/6"></div>
                <div class="h-3 bg-white/3 rounded w-2/3"></div>
              </div>
            </div>

            <!-- Hover overlay -->
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/30 transition-all duration-700 flex items-center justify-center">
              <div class="opacity-0 group-hover:opacity-100 transition-opacity duration-500 translate-y-4 group-hover:translate-y-0">
                <div class="w-16 h-16 rounded-full border border-accent flex items-center justify-center bg-accent/10 backdrop-blur-sm">
                  <svg class="w-5 h-5 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 17L17 7M17 7H7M17 7v10" />
                  </svg>
                </div>
              </div>
            </div>
          </div>

          <!-- Project info -->
          <div class="mt-5 flex items-start justify-between">
            <div>
              <h3 class="text-lg font-semibold group-hover:text-accent transition-colors duration-500">{project.title}</h3>
              <p class="text-text-secondary text-sm mt-1">{project.category}</p>
            </div>
            <span class="text-text-muted text-sm font-mono">{project.year}</span>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<!-- Horizontal scroll script -->
<script>
  const track = document.getElementById('portfolio-track');
  const container = document.getElementById('portfolio-container');

  if (track && container) {
    let scrollAmount = 0;
    const speed = 0.5;

    window.addEventListener('scroll', () => {
      const rect = container.getBoundingClientRect();
      const viewportHeight = window.innerHeight;

      // Calculate how far into the section we are
      if (rect.top < viewportHeight && rect.bottom > 0) {
        const progress = (viewportHeight - rect.top) / (viewportHeight + rect.height);
        const maxScroll = track.scrollWidth - container.clientWidth;
        scrollAmount = progress * maxScroll * speed;
        track.style.transform = `translateX(-${Math.max(0, scrollAmount)}px)`;
      }
    });
  }
</script>
