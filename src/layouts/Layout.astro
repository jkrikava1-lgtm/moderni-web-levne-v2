---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  noLoader?: boolean;
}

const { title, description = 'Tvorba webových stránek pro firmy, živnostníky a řemeslníky. Profesionální web na míru od 9 900 Kč, hotovo do 3 týdnů. Žádné měsíční poplatky, žádné šablony.', noLoader = false } = Astro.props;
const siteUrl = 'https://hezkewebovky.cz';
const ogImage = `${siteUrl}/og-image-v2.png`;
---

<!doctype html>
<html lang="cs">
  <head>
    <!-- Google Analytics with Consent Mode v2 -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-0RNN8D6XDS"></script>
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}

      // Default: deny all until user consents
      gtag('consent', 'default', {
        'ad_storage': 'denied',
        'ad_user_data': 'denied',
        'ad_personalization': 'denied',
        'analytics_storage': 'denied'
      });

      // Apply saved consent if exists
      var savedConsent = localStorage.getItem('cookie-consent');
      if (savedConsent === 'accepted') {
        gtag('consent', 'update', {
          'analytics_storage': 'granted'
        });
      }

      gtag('js', new Date());
      gtag('config', 'G-0RNN8D6XDS');
    </script>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="author" content="Hezké webovky" />
    <meta name="robots" content="index, follow" />
    <meta name="seznam-wmt" content="DjOnAjo8ks1uVCH3UahjOEJcLJfp8gdU" />
    <meta name="seznam-wmt" content="QJQXL1hY4eL54snsMbWlXMaLk7ClN3gw" />
    <link rel="canonical" href={siteUrl} />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="sitemap" href="/sitemap-index.xml" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={siteUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:locale" content="cs_CZ" />
    <meta property="og:site_name" content="Hezké webovky" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <!-- Fonts — non-blocking -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Nunito:wght@300;700&family=Instrument+Serif:ital@0;1&display=swap" rel="stylesheet" media="print" onload="this.media='all'" />
    <noscript><link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Nunito:wght@300;700&family=Instrument+Serif:ital@0;1&display=swap" rel="stylesheet" /></noscript>

    <title>{title}</title>

    <!-- Structured Data: LocalBusiness -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "LocalBusiness",
      "name": "Hezké webovky",
      "description": "Tvorba profesionálních webových stránek za férovou cenu. Weby na míru pro firmy, živnostníky a řemeslníky. Ceny od 9 900 Kč, hotovo do 3 týdnů.",
      "url": siteUrl,
      "telephone": "+420777724573",
      "email": "info@hezkewebovky.cz",
      "priceRange": "9 900 Kč – 50 000 Kč",
      "areaServed": { "@type": "Country", "name": "Česká republika" },
      "hasOfferCatalog": {
        "@type": "OfferCatalog",
        "name": "Webové služby",
        "itemListElement": [
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Tvorba webových stránek na míru" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Redesign webu" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "SEO optimalizace" } },
          { "@type": "Offer", "itemOffered": { "@type": "Service", "name": "Správa a údržba webu" } }
        ]
      },
      "sameAs": [
        "https://www.facebook.com/hezkewebovky",
        "https://www.instagram.com/hezkewebovky"
      ]
    })} />
    </script>

    <!-- Structured Data: FAQPage -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        { "@type": "Question", "name": "Kolik stojí vytvoření webových stránek?", "acceptedAnswer": { "@type": "Answer", "text": "Cena za kompletní web začíná na 9 900 Kč. V ceně je návrh designu, programování, základní SEO optimalizace a nasazení. Přesnou cenu vždy dohodnu předem — žádné skryté poplatky." } },
        { "@type": "Question", "name": "Jak dlouho trvá vytvoření webu?", "acceptedAnswer": { "@type": "Answer", "text": "Standardní web hotový do 3 týdnů od schválení návrhu. Termín vždy dohodnu předem." } },
        { "@type": "Question", "name": "Pro koho weby tvořím?", "acceptedAnswer": { "@type": "Answer", "text": "Primárně pro živnostníky, řemeslníky a malé firmy, které potřebují profesionální web bez agenturnich cen — elektrikáři, instalatéři, restaurace, kosmetičky nebo e-shopy." } },
        { "@type": "Question", "name": "Co je součástí ceny webu?", "acceptedAnswer": { "@type": "Answer", "text": "Návrh a programování webu na míru, mobilní verze (responzivní design), základní SEO optimalizace, nasazení na server, zaškolení a podpora po spuštění." } },
        { "@type": "Question", "name": "Musím platit měsíční poplatky za web?", "acceptedAnswer": { "@type": "Answer", "text": "Ne. Platíte jednorázově za hotový web. Jediné pravidelné náklady jsou doména (cca 200 Kč/rok) a hosting (cca 500–1000 Kč/rok)." } },
        { "@type": "Question", "name": "Dělám e-shopy?", "acceptedAnswer": { "@type": "Answer", "text": "Ano, menší e-shopy dělám. Pro větší projekty s tisíci produktů doporučím vhodné řešení a pomohu s nastavením." } }
      ]
    })} />
    </script>
  </head>
  <body class="antialiased">
    <!-- Cookie consent banner — centered modal -->
    <div id="cookie-banner" class="fixed inset-0 z-[9999] hidden">
      <div class="absolute inset-0 bg-black/70 backdrop-blur-sm"></div>
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[calc(100%-2rem)] max-w-sm bg-dark-card border border-dark-border rounded-2xl shadow-2xl p-7">
        <div class="w-10 h-10 rounded-xl bg-accent/15 flex items-center justify-center mb-5">
          <svg class="w-5 h-5 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
          </svg>
        </div>
        <h2 class="text-lg font-bold text-white mb-2">Tento web používá cookies</h2>
        <p class="text-text-secondary text-sm leading-relaxed mb-6">
          Analytické cookies nám pomáhají pochopit, jak web používáte, a zlepšovat ho. Žádné reklamní ani sledovací cookies nepoužíváme.
        </p>
        <div class="space-y-2.5">
          <button id="cookie-accept" class="w-full py-3 bg-accent text-black text-sm font-bold rounded-lg hover:bg-white transition-all">Přijmout vše</button>
          <button id="cookie-settings" class="w-full py-2.5 text-sm text-text-secondary hover:text-white border border-dark-border hover:border-white/30 rounded-lg transition-all">Nastavení</button>
        </div>
      </div>
    </div>

    <!-- Cookie modal -->
    <div id="cookie-modal" class="fixed inset-0 z-[10000] hidden">
      <div class="absolute inset-0 bg-black/60 backdrop-blur-sm"></div>
      <div class="absolute bottom-0 left-0 right-0 sm:bottom-auto sm:top-1/2 sm:-translate-y-1/2 sm:left-1/2 sm:-translate-x-1/2 sm:w-full sm:max-w-lg bg-dark-card border border-dark-border sm:rounded-xl shadow-2xl p-6">
        <div class="flex items-start justify-between mb-1">
          <h3 class="text-xl font-bold text-white">Nastavení cookies</h3>
          <span class="text-xs text-text-secondary pt-1.5"><span id="active-count">2</span> z 2 aktivní</span>
        </div>
        <p class="text-sm text-text-secondary mb-6">Vyberte, které cookies povolíte. Nezbytné jsou vždy aktivní.</p>
        <div class="space-y-4 mb-6">
          <div class="flex items-start justify-between gap-4 py-3 border-b border-dark-border">
            <div>
              <p class="text-sm font-semibold text-white/80">Nezbytné</p>
              <p class="text-xs text-text-secondary mt-0.5">Zajišťují základní funkce webu. Nelze vypnout.</p>
            </div>
            <div class="flex-shrink-0 w-10 h-6 bg-accent rounded-full opacity-40 cursor-not-allowed relative mt-0.5">
              <div class="absolute right-1 top-1 w-4 h-4 bg-black rounded-full"></div>
            </div>
          </div>
          <div class="flex items-start justify-between gap-4 py-3">
            <div class="flex-1">
              <p class="text-sm font-semibold text-white/80">Analytické <span class="text-xs text-accent font-normal ml-1">(doporučeno)</span></p>
              <p class="text-xs text-text-secondary mt-0.5">Google Analytics — pomáhají nám zlepšovat web.</p>
              <p id="analytics-warning" class="hidden text-xs text-amber-400/80 mt-1.5">Bez analytiky nemůžeme sledovat, co vám chybí, a web zlepšovat pro vás.</p>
            </div>
            <button id="toggle-analytics" role="switch" aria-checked="true"
              class="flex-shrink-0 w-10 h-6 rounded-full transition-colors duration-200 bg-accent relative mt-0.5">
              <div id="toggle-knob" class="absolute top-1 w-4 h-4 bg-black rounded-full shadow transition-all duration-200 right-1"></div>
            </button>
          </div>
        </div>
        <div id="normal-buttons">
          <div class="flex gap-3">
            <button id="cookie-save" class="flex-1 py-2.5 text-sm font-bold text-black bg-accent hover:bg-white rounded-lg transition-all">Uložit nastavení</button>
            <button id="cookie-accept-all" class="flex-1 py-2.5 text-sm font-semibold text-text-secondary border border-dark-border hover:text-white hover:border-text-secondary rounded-lg transition-all">Přijmout vše</button>
          </div>
        </div>
        <div id="confirm-area" class="hidden">
          <div class="bg-dark-card border border-dark-border rounded-lg p-4">
            <p class="text-sm text-text-secondary text-center mb-3">Opravdu chcete omezit naše možnosti zlepšování webu?</p>
            <button id="confirm-accept" class="w-full py-2.5 text-sm font-bold text-black bg-accent hover:bg-white rounded-lg transition-all mb-2">Přijmout doporučené cookies</button>
            <button id="confirm-reject" class="w-full py-1.5 text-xs text-text-secondary/50 hover:text-text-secondary transition-colors">Pokračovat bez analytiky</button>
          </div>
        </div>
      </div>
    </div>

    <script is:inline>
      (function() {
        var analyticsOn = true;
        var confirmStep = false;
        var banner = document.getElementById('cookie-banner');
        var saved = localStorage.getItem('cookie-consent');
        if (saved === 'accepted' || saved === 'analytics') {
          if (typeof gtag === 'function') gtag('consent', 'update', { 'analytics_storage': 'granted' });
        } else if (!saved) {
          banner.classList.remove('hidden');
        }

        function hideBanner() {
          banner.classList.add('hidden');
          document.getElementById('cookie-modal').classList.add('hidden');
        }
        ['cookie-accept','cookie-accept-all'].forEach(function(id) {
          document.getElementById(id)?.addEventListener('click', function() {
            localStorage.setItem('cookie-consent', 'accepted');
            if (typeof gtag === 'function') gtag('consent', 'update', { 'analytics_storage': 'granted' });
            hideBanner();
          });
        });
        document.getElementById('cookie-settings')?.addEventListener('click', function() {
          document.getElementById('cookie-modal').classList.remove('hidden');
        });
        document.getElementById('toggle-analytics')?.addEventListener('click', function() {
          analyticsOn = !analyticsOn;
          confirmStep = false;
          this.classList.toggle('bg-accent', analyticsOn);
          this.classList.toggle('bg-dark-border', !analyticsOn);
          var k = document.getElementById('toggle-knob');
          k.classList.toggle('right-1', analyticsOn);
          k.classList.toggle('left-1', !analyticsOn);
          this.setAttribute('aria-checked', String(analyticsOn));
          var cnt = document.getElementById('active-count');
          if (cnt) cnt.textContent = analyticsOn ? '2' : '1';
          var warn = document.getElementById('analytics-warning');
          if (warn) warn.classList.toggle('hidden', analyticsOn);
          var saveBtn = document.getElementById('cookie-save');
          if (saveBtn) {
            if (analyticsOn) {
              saveBtn.textContent = 'Uložit nastavení';
              saveBtn.className = 'flex-1 py-2.5 text-sm font-bold text-black bg-accent hover:bg-white rounded-lg transition-all';
            } else {
              saveBtn.textContent = 'Uložit bez doporučených';
              saveBtn.className = 'flex-1 py-2.5 text-sm font-semibold text-text-secondary/50 bg-dark-border rounded-lg transition-all';
            }
          }
          document.getElementById('confirm-area').classList.add('hidden');
          document.getElementById('normal-buttons').classList.remove('hidden');
        });
        document.getElementById('cookie-save')?.addEventListener('click', function() {
          if (analyticsOn) {
            localStorage.setItem('cookie-consent', 'analytics');
            if (typeof gtag === 'function') gtag('consent', 'update', { 'analytics_storage': 'granted' });
            hideBanner();
            return;
          }
          if (!confirmStep) {
            confirmStep = true;
            document.getElementById('normal-buttons').classList.add('hidden');
            document.getElementById('confirm-area').classList.remove('hidden');
          }
        });
        document.getElementById('confirm-accept')?.addEventListener('click', function() {
          localStorage.setItem('cookie-consent', 'accepted');
          if (typeof gtag === 'function') gtag('consent', 'update', { 'analytics_storage': 'granted' });
          hideBanner();
        });
        document.getElementById('confirm-reject')?.addEventListener('click', function() {
          localStorage.setItem('cookie-consent', 'rejected');
          hideBanner();
        });
        document.getElementById('cookie-modal')?.addEventListener('click', function(e) {
          if (e.target === this || e.target === this.firstElementChild) hideBanner();
        });
      })();
    </script>

    <!-- Loading screen — desktop only via JS -->
    {!noLoader && (
      <div class="loader hidden" id="loader">
        <div class="loader-counter" id="loader-counter"></div>
      </div>
    )}

    <!-- Custom cursor — desktop only, hidden via CSS on touch -->
    <div class="cursor-dot" id="cursor-dot"></div>
    <div class="cursor-ring" id="cursor-ring"></div>

    <slot />

    <!-- ALL JS EFFECTS — desktop only -->
    <script>
      const isDesktop = window.matchMedia('(pointer: fine)').matches;

      // === RANDOM GREEN WORD IN HEADINGS — mobile/tablet ===
      if (!isDesktop) {
        document.querySelectorAll('#sluzby h2, #cena h2, #ukazky h2, #jaknato h2, #kontakt h2').forEach((h2) => {
          const words = h2.textContent?.trim().split(/\s+/) || [];
          if (words.length > 1) {
            const idx = Math.floor(Math.random() * words.length);
            h2.innerHTML = words.map((w, i) => i === idx ? `<span class="accent-word">${w}</span>` : w).join(' ');
          }
        });
      }

      // === LOADER ===
      const loader = document.getElementById('loader');
      if (loader) {
        if (isDesktop && !window.location.hash) {
          loader.classList.remove('hidden');
          const counter = document.getElementById('loader-counter');
          counter.innerHTML = `0<span>%</span>`;
          let count = 0;
          const interval = setInterval(() => {
            count += Math.floor(Math.random() * 8) + 2;
            if (count >= 100) {
              count = 100;
              clearInterval(interval);
              if (counter) counter.innerHTML = `100<span>%</span>`;
              setTimeout(() => loader.classList.add('hidden'), 400);
            }
            if (counter) counter.innerHTML = `${count}<span>%</span>`;
          }, 40);
        }
      }

      // === LENIS SMOOTH SCROLL — desktop only ===
      if (isDesktop) {
        import('lenis').then(({ default: Lenis }) => {
          const lenis = new Lenis({
            duration: 1.2,
            easing: (t: number) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
          });
          function raf(time: number) {
            lenis.raf(time);
            requestAnimationFrame(raf);
          }
          requestAnimationFrame(raf);
        });
      }

      // === CUSTOM CURSOR — desktop only ===
      if (isDesktop) {
        const dot = document.getElementById('cursor-dot') as HTMLElement;
        const ring = document.getElementById('cursor-ring') as HTMLElement;
        let mouseX = 0, mouseY = 0;
        let ringX = 0, ringY = 0;

        document.addEventListener('mousemove', (e) => {
          mouseX = e.clientX;
          mouseY = e.clientY;
          if (dot) { dot.style.left = mouseX + 'px'; dot.style.top = mouseY + 'px'; }
        });

        function animateRing() {
          ringX += (mouseX - ringX) * 0.12;
          ringY += (mouseY - ringY) * 0.12;
          if (ring) { ring.style.left = ringX + 'px'; ring.style.top = ringY + 'px'; }
          requestAnimationFrame(animateRing);
        }
        animateRing();

        document.querySelectorAll('a, button, [data-hover]').forEach((el) => {
          el.addEventListener('mouseenter', () => document.body.classList.add('cursor-hover'));
          el.addEventListener('mouseleave', () => document.body.classList.remove('cursor-hover'));
        });
      }

      // === SCROLL REVEAL — desktop only ===
      if (isDesktop) {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) entry.target.classList.add('visible');
            });
          },
          { threshold: 0.1 }
        );
        document.querySelectorAll('.reveal').forEach((el) => observer.observe(el));
      }

      // === PARALLAX on section headings — desktop only ===
      if (isDesktop) {
        const parallaxHeadings = document.querySelectorAll('h2');
        window.addEventListener('scroll', () => {
          parallaxHeadings.forEach((h2) => {
            const rect = h2.getBoundingClientRect();
            const speed = 0.08;
            const offset = (rect.top - window.innerHeight / 2) * speed;
            (h2 as HTMLElement).style.transform = `translateY(${offset}px)`;
          });
        });
      }

      // === MAGNETIC BUTTONS — desktop only ===
      if (isDesktop) {
        document.querySelectorAll('.magnetic-btn').forEach((btn) => {
          btn.addEventListener('mousemove', (e) => {
            const rect = (btn as HTMLElement).getBoundingClientRect();
            const x = e.clientX - rect.left - rect.width / 2;
            const y = e.clientY - rect.top - rect.height / 2;
            (btn as HTMLElement).style.transform = `translate(${x * 0.3}px, ${y * 0.3}px)`;
          });
          btn.addEventListener('mouseleave', () => {
            (btn as HTMLElement).style.transform = 'translate(0, 0)';
          });
        });
      }
    </script>
  </body>
</html>
